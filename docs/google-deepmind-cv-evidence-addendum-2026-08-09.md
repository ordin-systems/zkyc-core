# zKYC / zkYA Google DeepMind CV Evidence Addendum

**Status:** post-CV candidate evidence

**Prepared:** 2026-08-09

**Candidate:** local `v0.3.0` implementation baseline `1a15de8117ea27672fbd7fffa1c63f633ca3b9bd`

This checked-in addendum reconciles dated evidence layers. It does not replace, edit, backdate, or reinterpret the frozen CV. Post-CV implementation does not retroactively alter deadline-time evidence.

## Canonical authorship

Mike “Mizzy” Barrera and Monique Abrams — joint authors and co-architects.

No separate responsibility split is asserted.

## Layer 1 — frozen deadline-time CV evidence

- Frozen PDF: `Mizzy_CV_CURRENT_LIVE_WITH_LINK_SECTION_2026-08-08.pdf`
- Frozen date: 2026-08-08
- SHA-256: `30746da308e4fa9c149c68b893f1a9bb922bc3a3e63fb3776e8ebd84d1c2ab9e`

The PDF remains outside this repository and unchanged.

Safe deadline-time zkYA wording remains:

> Co-developed the zkYA / Know-Your-Agent product concept and strategy with Monique Abrams.

The frozen CV's zKYC role dates and title are not established by source code. Its `August 2025–July 2026` date range conflicts with a separately recorded `August 2025–present` profile and must be reconciled against independent role records before any later CV revision. This addendum does not choose or attest a date.

## Layer 2 — historical v0.2.1 release evidence

Historical immutable release: `v0.2.1-full-stack-reference` at commit `03962019372444fb18affbbbdf4597dc04a1100a`.

That release remains unchanged. It supports a bounded local full-stack authority reference with credential-bound affiliations/capabilities, reason-coded decisions, human step-up, HMAC receipts, one-time in-memory consumption, Hono API, TypeScript SDK, and operator UI.

It does not contain executable principal-type participation, an explicit grantor/delegate delegation object, separate delegated credentials, or an executable zkYA onboarding application.

## Controlling 2026-08-09 external audit

- Document: `GOOGLE_DEEPMIND_CV_RELEASE_LEVEL_AUDIT_2026-08-09.md`
- SHA-256: `580bf6e9a689dd1fbda26342e307a39e6042671edcd8be5182c8c5612fd1a9ba`

The audit truthfully evaluated the frozen CV against historical evidence available to it, including v0.2.1 and zkYA concept/strategy sources. The later candidate addresses identified technical gaps, but it does not retroactively change that audit's date-specific findings.

## Layer 3 — post-CV v0.3 candidate implementation

Executable baseline `1a15de8117ea27672fbd7fffa1c63f633ca3b9bd` adds post-CV candidate evidence for:

- exact `HUMAN`, `ORGANIZATION`, and `AGENT` principal types;
- credential v2 principal-type, affiliation, capability, action, resource, issuer, validity, and scope bindings;
- explicit `DIRECT` and `DELEGATED` modes;
- registered one-hop delegations binding distinct grantor/delegate identities, grantor credential, policy ID/version, validity, and attenuated capability/action/resource scope;
- a separate acting-subject identity credential and grantor credential, without grantor-affiliation transfer;
- delegation/credential/policy expiry and revocation revalidation;
- human-only exact-scope step-up request, resolution, authorization, and consumption;
- authority-bound HMAC receipt v2 and one-time replay protection;
- strict SDK runtime validation and request correlation, executable versioned lifecycle transcripts, and retained onboarding views;
- both the operator cockpit and a dedicated zkYA onboarding UI;
- one real local Chromium path through the built zkYA UI, SDK, and loopback API;
- fail-closed scanner/archive/package verification with blank-consumer package proof.

Verified behavioral counts at that executable baseline are core 45, API/server 13, SDK 10, operator UI 3, zkYA component 9, scanner regression 7, release-tooling regression 6, and Chromium E2E 1. Typechecks, builds, format, security, package, and audit gates are separate checks.

This is integrated local candidate evidence only. It has not been pushed, merged, tagged, released, published, independently approved, or published to npm. It is not deadline-time evidence for the frozen CV.

## Candidate claim reconciliation

The post-CV implementation can support a later, date-qualified statement that an executable zkYA onboarding reference exists, but only as candidate evidence until exact release verification. It must remain separate from the safe deadline-time product-concept sentence above.

A bounded zKYC implementation description may name typed principals, one-hop delegated authority, exact action/resource scope, policy pinning, human step-up, and HMAC receipt v2 only when it explicitly identifies this post-CV candidate or a later verified exact release.

Neither code nor release evidence establishes employment title, employment dates, customers, adoption, independent validation, or a responsibility split between the joint authors/co-architects.

## Layer 4 — later exact-tag receipt

After an exact tag exists, a separate verification receipt may record:

- tag-to-commit and tree resolution;
- exact environment and lockfile hash;
- actual command results and behavioral counts;
- hashes of already-constructed release assets.

No future-event fields are to be filled later inside the tag. The tagged source cannot truthfully contain proof of later publication or its own final outer-archive digest.

## Layer 5 — later post-publication logged-out audit

Only after publication may a separately dated sidecar record:

- GitHub release/API state, including any immutable setting actually read back;
- logged-out public availability and downloads;
- recomputed checksums, extraction, and content readback;
- any public CI/tag evidence that actually exists.

This addendum contains none of those claims and provides no public v0.3 URL.

## Maturity boundary

The candidate does not establish production identity/KYC/AML, zero-knowledge verification, authentication, deployment, durable/distributed state, protected execution, adoption, scale, external validation, or independent review.
